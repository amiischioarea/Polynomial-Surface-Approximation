# Polynomial-Surface-Approximation
This project focuses on modeling an unknown function by approximating a surface defined by two input variables and one output variable. The primary goal is to build a robust model using two-dimensional polynomial regression to fit measured data points as accurately as possible.
