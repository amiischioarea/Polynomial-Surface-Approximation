This project implements a system for modeling unknown functions by approximating a surface defined by two input variables (x1, x2) and one output variable (y). The solution utilizes two-dimensional polynomial regression to achieve a high-fidelity fit to measured data.

The model effectively balances complexity and precision. By automatically choosing the optimal degree, the system produces reconstructed surfaces that are nearly identical to the original ones with minimal error.
